# MyMany — юзер-кейсы (живой список)

**Проект переделан 2026-09-06:** из «монитора крипторынка» (CoinGecko) → **арбитражная база цепочек обмена**
(данные BestChange через ratescout). Старые UC-01…06 (рыночный дашборд) неактуальны — заменены ниже.

## UC-A1. Монитор арбитражных цепочек на главной ✅
**Предусловие:** посетитель открывает my-many.ru.
**Шаги:** 1) открыть `/`; 2) видит статы базы (сколько цепочек/валют, лучшая доходность) + таблицу цепочек;
3) переключает режим (2/3/4 звена), сортировку (доходность/риск), фильтр по стартовой валюте; 4) «показать ещё»;
5) клик по цепочке → страница цепочки.
**Ожидаемо:** монитор в стиле ratescout (его styles.css): путь цепочки (BTC→…→BTC), доходность+мини-бар, риск (бейдж+бар),
мин. обменников. Топ-800 встроены в HTML (быстро, индексируется). Дата обновления + «данные BestChange».
**РАДИУС:** `build.py` (`render_home`, `HOME_JS`). Данные из `compute_shards`. СОСЕДИ: ratescout не затронут (только читаем его rates.json).
**Проверка:** build EXIT 0 (18 444 цепочки/166 валют); node --check HOME_JS OK; DOM-стаб → 60 строк, 120 мини-баров, 60 ссылок на /c/. **Статус:** ✅ (визуальная приёмка на проде — за владельцем).

## UC-A2. Страница цепочки + калькулятор ✅
**Предусловие:** клик по цепочке в мониторе или на странице валюты (`/c/?s=<slug>&i=<index>`).
**Шаги:** 1) страница грузит шард `data/chains/<slug>.json`, берёт цепочку по индексу; 2) показывает пошаговую конверсию
(на каждом шаге: отдаёте / курс / получаете, ссылка шага → BestChange для реального обмена); 3) пользователь вводит сумму в
стартовой валюте → калькулятор считает, сколько получит по текущим курсам и итоговую прибыль (сумма и %).
**Ожидаемо:** итог калькулятора = заявленной доходности (проверено: 0 расхождений на 120 цепочках; курсы — полный float).
Перелинковка: стартовая валюта → ratescout `/valuta/<slug>/`, CTA «обменять на RateScout». (Кнопка «Арбитраж» живёт на ratescout `/tsepochki/`, не тут — см. ratescout usecases.)
**График «сумма и процент по шагам в USDT»:** над таблицей шагов — SVG-линия накопленного % по узлам цепочки (x=валюты пути, y=итог% в USDT), точки с наведением {валюта, ≈сумма в USDT, итог%}; в таблицу шагов добавлены колонки «≈ USDT» и «Итог %» (обновляются с суммой калькулятора). Цена USDT хранится в узле шарда (n[3]); итог последнего шага = c.p (сверено). Видно, на каком шаге набегает прибыль.
**График доходности во времени:** над калькулятором — SVG-график с **вкладками диапазона (24ч/3д/7д/14д/30д, как у валют на ratescout; недоступные при нехватке точек — гасятся)**; как менялась доходность цепочки; при наведении тултип {дата UTC, валюты цепочки, доходность %, цена старт-валюты в USDT}. 🔴 Историю обменных курсов мы НЕ хранили → прошлое не восстановить; пишем ВПЕРЁД: `main()` копит `data/chain-history.json` (+шарды `chist/<slug>.json`) — точка на каждое обновление данных (rates.generated_at, ~раз в час), топ-800 цепочек, 168 точек (~7д). Персист между сборками — фетчем прошлого файла с живого сайта (deploy коммитов не делает). <2 точек → заметка «накапливается».
**РАДИУС:** `build.py` (`render_detail_page`, `DETAIL_JS` c `drawChart`, история в `main()`) + шарды `chains/` и `chist/`. **Проверка:** build EXIT 0; node --check DETAIL_JS OK; калькулятор сверен; график прогнан в Node-стабе (svg+polyline 3 точки+сетка). **Статус:** ✅ (график наполняется со временем; визуал — за владельцем).

## UC-A3. Страницы входа по стартовой валюте (SEO) ✅
**Предусловие:** «у меня есть BTC» → `/valuta/bitcoin/` (или любая валюта с цепочками).
**Шаги:** открыть страницу → SSR-таблица всех цепочек, начинающихся с этой валюты (путь/доходность/риск/обменников/тип),
клик → страница цепочки; перелинковка на ratescout `/valuta/<slug>/`.
**Ожидаемо:** 166 страниц (по валютам с ≥1 цепочкой), в sitemap (hourly, 0.7). Уникальный интент «арбитраж с X».
**РАДИУС:** `build.py` (`render_currency`, sitemap). **Проверка:** build EXIT 0, 166 стр.; bitcoin/index.html ссылается на /c/?s=bitcoin. **Статус:** ✅.

## UC-A4. Данные из ratescout + автодеплой 15 мин + keepalive
**Предусловие:** ratescout публикует `https://ratescout.ru/rates.json` (добавлен экспорт в его build.py).
**Шаги:** `deploy.yml` cron `*/15` (+сдвиг) + push + dispatch → `build.py` тянет rates.json (ratescout.ru), history/currencies
(raw-GitHub), фолбэк на кэш → dist → Pages. `keepalive.yml` — недельный PAT-коммит (чтобы крон не уснул за 60 дней).
**Ожидаемо:** база обновляется автоматически. 🧭 «15 мин» = частота сборки; данные свежеют с обновлением rates.json (~ежечасно).
**РАДИУС:** `mymany/.github/workflows/deploy.yml` (cron) + `ratescout/build.py` (экспорт rates.json). **Статус:** ✅ код; ⏳ первый деплой после публикации rates.json на ratescout.
**Долг:** ⏳ автопостинг по API (доп. к keepalive) — ждём ключ/цель от владельца.

## UC-A5. Интерфейс/бренд/перелинковка как у ratescout ✅
**Ожидаемо:** DOS-тема ratescout (styles.css, #wrapper/#header/#topnav/#footer), в футере «MyMany — проект RateScout по
арбитражу» + ссылки на ratescout (обмен/направления/курсы/тепловая карта) + дисклеймер (доходность теоретическая, 18+).
Счётчики Я.Метрика 111586112 + GA G-PPN27D6JXS и Яндекс-тег `d5dd2e5c5d4ee324` — сохранены. **Статус:** ✅.

## UC-A6. Аккуратный футер (стиль ratescout) ✅
**Ожидаемо:** на ВСЕХ страницах (через `foot()`) — три чистых блока по образцу ratescout, без дублей:
`.disc` (краткий дисклеймер: проект RateScout, доходность теоретическая), `.links` (один ряд: обмен/направления/курсы/
тепловая карта/цепочки + О сервисе/AML/Виджет/О редакции/Блог/Telegram/Mastodon/Yell.ru/Раскрытие/Политика — абсолютные
на ratescout.ru), `.fine` (одна строка: «18+… © MyMany · my-many.ru — проект RateScout. Владелец: … ИНН 381616884622»).
Классы стилизует общий styles.css. Убраны повторы «18+»/двойной копирайт/`<br>`-свалка прежней версии. LiveInternet не копируем.
**РАДИУС:** `build.py` (`foot()`). **Проверка:** build EXIT 0; на index/c/valuta по 1×disc/links/fine, один © в футере. **Статус:** ✅.

## UC-A7. Защита ветки main без поломки крона ✅
**Предусловие:** секрет `GH_PAT` (admin: classic `repo` или fine-grained Administration:write) добавлен в репо.
**Шаги:** Actions → «Protect main (cron-safe)» → Run workflow → PUT `/branches/main/protection` через GH_PAT.
**Ожидаемо:** на `main` — запрет force-push/удаления + enforce_admins, но БЕЗ required PR/checks (обычный push keepalive/deploy проходит → scheduled-воркфлоу живут). HTTP 200 = включено; 401/403 = у токена нет admin.
**РАДИУС:** `.github/workflows/protect.yml`. СОСЕДИ: `keepalive.yml` (недельный PAT-коммит) и `deploy.yml` (пуш Pages) не ломаются — прямой push разрешён. **Проверка:** ✅ ВКЛЮЧЕНО 2026-09-06 самозапуском (push-триггер+GH_PAT): PUT protection → HTTP 200, enabled:true; обычный push после этого проходит (force-push/удаление блокируются, PR не требуется) → keepalive/deploy живут. Тест-артефакты убраны, workflow оставлен ручным (переприменение). **Статус:** ✅ включено и проверено.

## UC-A8. Страница 404 + SEO-оптимизация ✅
**404:** `render_404()` → `dist/404.html` (GitHub Pages отдаёт на несуществующие пути) в общем стиле (header/footer), `noindex, follow`, кнопка «в монитор» + популярные точки входа + CTA на ratescout.
**SEO (в `head()` и рендерах):** og:image + twitter card на всех страницах; canonical (условный); `/c/` = `noindex, follow` (query-шаблон, чтобы не плодить тонкие дубли); на страницах валют — **BreadcrumbList** + расширенный CollectionPage; на главной — блок «Популярные точки входа» (40 внутренних ссылок на `/valuta/`) и текст «Что такое арбитраж» (уникальный контент); sitemap = главная(1.0)+валюты(0.7) hourly; robots allow+sitemap.
**РАДИУС:** `build.py` (`head`, `render_404`, `render_home`, `render_currency`, `render_detail_page`, main). **Проверка:** build EXIT 0; JSON-LD валиден (WebSite/CollectionPage+BreadcrumbList/WebApplication); 404 noindex; 40 внутр. ссылок с главной; og:image/twitter на index. **Статус:** ✅ (индексация — за поисковиками/владельцем).

## UC-A9. Мобильная версия ✅
**Предусловие:** открыть my-many.ru с телефона (viewport meta уже есть).
**Ожидаемо:** сайт читаем на узком экране. 🔴 Главный фикс: ratescout `styles.css` на ≤760px делает `#topnav{display:none}` (у него мобильное меню = бургер `#drawer`+app.js, которых у my-many НЕТ) → без правки на мобиле пропадало ВСЁ меню. В `SUPP_CSS` добавлен `@media(max-width:760px)`: `#topnav{display:block!important}` (меню возвращено, `#menu-top` flex-wrap), статы 2-в-ряд, контролы монитора на всю ширину, таблица меньше шрифт + путь переносится, тултип графика `max-width:80vw`, поля калькулятора на всю ширину, шаг-таблица компактнее; `@media(max-width:460px)` прячет мини-бары и статы в столбик.
**РАДИУС:** `build.py` (`SUPP_CSS`). СОСЕДИ: базовая адаптивность (#wrapper fluid, одна колонка) — из ratescout styles.css. **Проверка:** build EXIT 0; на всех страницах 2×@media + `#topnav{display:block}`; viewport есть. Визуал на устройстве — за владельцем. **Статус:** ✅.

## UC-A10. Английская версия (i18n RU/EN) ✅
**Предусловие:** `python3 build.py`; открыть `/en/` (и переключатель 🌐 в меню).
**Ожидаемо:** RU в корне (`/`), EN в `/en/` — как у ratescout. Полный перевод: шапка/меню/футер, монитор (`HOME_JS`), страница цепочки + калькулятор/график (`DETAIL_JS`, тексты в объекте `LBL`), страницы валют (SSR), 404, дисклеймеры. hreflang ru/en/x-default + `og:locale` + `<html lang>`; переключатель языка ведёт на тот же путь другой версии. Ссылки на ratescout — с `/en/`-префиксом (`rs(lang,path)`). Данные (`data/chains`,`data/chist`,`chain-history.json`) ОБЩИЕ (языконезависимы) — детальная фетчит `/data/…` абсолютно, ссылки монитора на `/c/` строятся как `LBL.cpref+"/c/"` → `/en/c/`. sitemap = RU+EN с `xhtml:link` alternate.
**РАДИУС:** `build.py` (`L`,`rs`,`rs_home`,`DISC`,`MODE_NAME`,`head`,`foot`,`home_lbl`,`detail_lbl`,`HOME_JS`,`DETAIL_JS`,`render_*`, main-цикл по `LANGS`, sitemap). СОСЕДИ: общие data-артефакты не дублируются на язык. **Проверка:** build EXIT 0 (21113 цепочек); `en/index.html` `<html lang="en">`+4×hreflang+🌐RU; `index.html` 🌐EN+`href="/en/"`; плейсхолдеров `__LBL__/__DATA__` = 0; EN `/c/` JS-переводы (You give/You get/Invested); canonical EN валюты = `/en/valuta/bitcoin/`; sitemap 465×`/en/`+930×xhtml:link; curl все маршруты (`/`,`/en/`,`/en/valuta/…`,`/en/c/?…`,`/valuta/…`) = 200. Кириллица в EN — только CSS-комменты (невидимы). 
**Авто-язык:** первый вход — автоопределение по языку браузера (не `ru*` → EN); ручная смена 🌐 сохраняется (`localStorage['mm_lang']`) и держится на всех страницах. Скрипт в `<head>` до отрисовки (без мигания), на 404 не ставится. `head(autoredir=True)` на контентных. Проверка: RU-стр. содержит редирект `if(s==='en')…/en+path`, EN-стр. — снятие `/en`; onclick сохраняет выбор; 404 без `location.replace` (0), контентные — по 1. **Кнопка языка — как на ratescout:** `<a class="langsw">EN/RU</a>` в `#header` рядом с логотипом (стиль `.langsw` из общего `styles.css` ratescout — жёлтый текст/циан-рамка), НЕ пункт меню, без эмодзи; `onclick` сохраняет выбор в `localStorage['mm_lang']` (app.js ratescout у нас нет). РАДИУС этой правки: `head()` (var `switch` в `#header`, убрана `<li>` из `#menu-top`, удалён мёртвый `.lang-sw` в `SUPP_CSS`). СОСЕДИ: `.langsw`/`#header` приходят из подключённого `ratescout/assets/styles.css` — свой CSS не нужен. Проверка: `langsw` в `#header`=да, в `#topnav`=нет; RU→«EN», EN→«RU»; `onclick` пишет `mm_lang`; эмодзи/li в меню=0.
**Ручной визуал EN — за владельцем.** **Статус:** ✅ собрано и проверено; фронт на приёмку владельцу.
